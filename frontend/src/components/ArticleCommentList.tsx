import { FC } from 'react';

type ArticleCommentListPropsType = {
  postId: string;
};

const ArticleCommentList: FC<ArticleCommentListPropsType> = ({ postId }) => {

  return (
    <>
    </>
  );
};

export default ArticleCommentList;